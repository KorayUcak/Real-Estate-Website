/**
 * X / Twitter, og:image'i otomatik devralmaz — ayrı bir twitter:image bekler.
 * Aynı görseli yeniden üretmek yerine OG bileşenini tekrar kullanıyoruz.
 */
export { alt, size, contentType, default } from "./opengraph-image";
